# BSF
The BackScatter Factor (BSF) Calculation Tool is a Python-based graphical application developed to compute BSF values for photon beams using Monte Carlo–derived spectral data and ISO-recommended phantom datasets.
